apiVersion: v1
kind: Service
metadata:
  name: my-dotnet-project
  namespace: namespace-workflow-1771162579886
  labels:
    app.kubernetes.io/name: my-dotnet-project
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: my-dotnet-project
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000